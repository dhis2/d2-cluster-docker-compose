connection.dialect = org.hibernate.dialect.PostgreSQLDialect
connection.driver_class = org.postgresql.Driver
connection.url = jdbc:postgresql://db/dhis
connection.username = dhis
connection.password = dhis

tracker.import.preheat.cache.enabled=off


# Redis configuration
redis.enabled = true
redis.host = redis

# Read replica 1 configuration
# Database connection URL, username and password
read1.connection.url = jdbc:postgresql://db-rr/dhis
read1.connection.username = dhis
read1.connection.password = dhis

active.read.replicas = 1
